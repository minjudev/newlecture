package com.newlecture.web.api.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController("apiHomeController")
public class HomeController { // 문서가 아닌 데이터를 주는 것이라면 이렇게 따로 package를 만들 수 있음

}
